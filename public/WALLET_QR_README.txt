Place your scan-and-pay QR images here (PNG) with these exact filenames (see src/config.js WALLET_QR_IMAGES):

  wallet-qr-phonepe.png
  wallet-qr-amazonpay.png
  wallet-qr-paytm.png

If a file is missing, the app still shows the UPI ID for manual entry.
