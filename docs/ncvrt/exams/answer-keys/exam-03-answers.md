# Exam 3 — Answer Key

## Section A — MCQ

1-c, 2-b, 3-b, 4-b, 5-b, 6-b, 7-a, 8-b, 9-b, 10-b, 11-b, 12-b, 13-a, 14-b, 15-b

## Section B — Model Answers

**16.** (a) `=SUM(B2:D2)` (b) `=IF(E2>=40,"Pass","Fail")` (c) `=E2/300*100` or similar

**17.** Relative A1 changes when copied (B1, C1). Absolute $A$1 stays fixed when formula copied down column.

**18.** Select name + marks columns → Insert → Column chart → add title and axis labels.

**19.** Table=collection of records. Record=one row. Field=column. Primary key=unique ID per record.

**20.** Create → Query Design → add tblStudent → drag fields → Fees criteria `>=3000` → Run.

## Section C — Key points

**21.** See notes file functions table — each with syntax and numeric example.

**22.** Design View fields + types → Primary Key → Datasheet data → Query Design with criteria → calculated field → Form Wizard → Report Wizard.

## Section D — Practical rubric

Excel: 8 rows data, formulas work when marks changed, IF grades correct, COUNTIF returns numbers, chart displays, print titles set.  
Access: table structure correct, 5 records, queries return expected rows, form opens, report prints preview.
